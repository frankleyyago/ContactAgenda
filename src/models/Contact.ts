class Contact {
  name: string
  email: string
  phone: number
  id: number

  constructor(name: string, email: string, phone: number, id: number) {
    this.name = name
    this.email = email
    this.phone = phone
    this.id = id
  }
}

export default Contact
